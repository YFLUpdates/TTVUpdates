// import SelectStreams from "./components/SMP/SelectStreams.js";

// setInterval(async () => {
//   const getSelected = await SelectStreams(api);

//   if (getSelected === null) {
//     return;
//   }

//   YFLSMP = getSelected;
// }, 3.5 * 60 * 1000);

// setTimeout(async () => {
//   const getSelected = await SelectStreams(api);

//   if (getSelected === null) {
//     return;
//   }

//   YFLSMP = getSelected;
// }, 1000);
